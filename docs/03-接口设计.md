# MES 接口设计（RESTful API）

## 1. 接口规范

- 基础路径：`/api/v1`
- 请求/响应：JSON
- 认证：JWT Bearer Token
- 分页格式：`{ page, page_size, total, total_pages, items[] }`
- 统一响应：`{ code, message, data, trace_id }`

## 2. 核心 API 列表

### 2.1 基础数据

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /materials | 物料列表 |
| POST | /materials | 新增物料 |
| GET | /materials/{id} | 物料详情 |
| PUT | /materials/{id} | 更新物料 |
| DELETE | /materials/{id} | 删除物料 |
| GET | /boms | BOM列表 |
| POST | /boms | 新增BOM |
| GET | /routings | 工艺路线列表 |
| POST | /routings | 新增工艺路线 |

### 2.2 工单管理

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /work-orders | 工单列表（支持多条件筛选） |
| POST | /work-orders | 创建工单 |
| GET | /work-orders/{id} | 工单详情（含工序进度） |
| PUT | /work-orders/{id} | 更新工单 |
| DELETE | /work-orders/{id} | 删除工单 |
| POST | /work-orders/{id}/release | 下达工单 |
| POST | /work-orders/{id}/hold | 暂停工单 |
| POST | /work-orders/{id}/resume | 恢复工单 |
| POST | /work-orders/{id}/close | 关闭工单 |
| POST | /work-orders/{id}/cancel | 取消工单 |
| POST | /work-orders/{id}/split | 工单拆分 |
| POST | /work-orders/{id}/rework | 返工工单 |
| POST | /work-orders/{id}/scrap | 工单报废 |

### 2.3 排产管理

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /scheduling/unscheduled-orders | 获取可排产工单（RELEASED且未排产） |
| POST | /scheduling/schedule | 排产：将工单分配到指定产线 |
| POST | /scheduling/batch-schedule | 批量排产 |
| POST | /scheduling/auto-schedule | 自动排产 |
| GET | /scheduling/line/{lineId}/scheduled-orders | 获取指定产线的已排产工单 |
| POST | /scheduling/unschedule/{workOrderId} | 取消排产 |
| POST | /scheduling/swap-order | 交换排产顺序 |
| GET | /scheduling/production-lines | 获取所有产线列表（供排产页面下拉选择） |

### 2.4 派工管理

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /dispatch/dispatch-step | 派工：将工序分配到指定工位 |
| POST | /dispatch/undispatch-step/{workOrderStepId} | 取消派工 |
| GET | /dispatch/line/{lineId}/today-tasks | 查询产线今日派工任务 |
| GET | /dispatch/line/{lineId}/workstations | 获取产线下可选工位 |

### 2.5 报工

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /work-reports | 报工记录列表 |
| POST | /work-reports | 提交报工 |
| GET | /work-reports/{id} | 报工详情 |
| PUT | /work-reports/{id} | 修改报工 |
| POST | /work-reports/pda-scan | PDA扫码报工 |
| POST | /work-reports/batch | 批量报工 |

### 2.6 质量管理

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /qc/inspections | 质检单列表 |
| GET | /qc/inspections/{id} | 质检单详情（含质检项） |
| POST | /qc/inspections | 创建质检单 |
| POST | /qc/inspections/{id}/items | 添加质检项 |
| POST | /qc/inspections/{id}/verify | 判定/审核 |
| POST | /qc/inspections/{id}/handle-nonconforming | 不合格品处理（让步接收/返工/报废） |
| GET | /qc/dashboard/stats | 今日质检统计 |
| GET | /qc/dashboard/pending | 待检列表 |
| GET | /qc/dashboard/recent-failed | 近期不合格品列表 |

### 2.7 质检点配置

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /qc-checkpoints/by-step/{stepId} | 查询某工序配置的质检点 |
| POST | /qc-checkpoints | 配置质检点 |
| DELETE | /qc-checkpoints/{id} | 取消配置质检点 |

### 2.8 追溯

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /trace/by-batch/{batchNo} | 按批次追溯 |
| GET | /trace/by-serial/{serialNo} | 按序列号追溯 |
| GET | /trace/forward/{materialId}/{batchNo} | 正向追溯（原料→成品） |
| GET | /trace/backward/{serialNo} | 反向追溯（成品→原料） |

### 2.9 设备

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /equipment | 设备列表 |
| GET | /equipment/{id} | 设备详情 |
| POST | /equipment | 添加设备 |
| PUT | /equipment/{id} | 更新设备 |
| DELETE | /equipment/{id} | 删除设备 |
| POST | /equipment/{id}/maintain | 保养记录 |
| POST | /equipment/{id}/fault | 设备报修 |
| GET | /equipment/{id}/oee | 获取OEE数据 |
| POST | /equipment/{id}/maintenance-plan | 创建保养计划 |
| GET | /equipment/{id}/maintenance-plans | 获取保养计划列表 |
| POST | /equipment/{id}/maintenance-plan/{planId}/complete | 完成保养 |

### 2.10 Andon 异常管理

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /andon/events | 获取所有异常事件 |
| GET | /andon/active | 获取活跃（未处理）异常事件 |
| POST | /andon/trigger | 触发异常事件 |
| POST | /andon/{id}/resolve | 处理异常事件 |

### 2.11 生产看板

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /dashboard/orders/today | 今日工单统计 |
| GET | /dashboard/orders/status | 工单状态分布 |
| GET | /dashboard/output | 产量实时统计 |
| GET | /dashboard/equipment | 设备状态看板 |

### 2.12 系统管理

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /auth/login | 登录 |

### 2.13 种子数据

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /seed | 执行种子数据初始化 |

### 2.14 AI 智能接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /ai/quality/alerts | 获取活跃预警列表 |
| POST | /ai/quality/analyze | 执行质量分析 |
| POST | /ai/quality/alerts/{id}/process | 处理预警 |
| GET | /ai/quality/history | 预警历史 |
| GET | /ai/scheduling/recommend/{workOrderId} | 获取排程推荐 |
| GET | /ai/equipment/health | 设备健康列表 |
| GET | /ai/equipment/health/{equipmentId} | 单设备健康分析 |
| GET | /ai/equipment/high-risk | 高风险设备 |

## 3. 接口示例

### 创建工单

```
POST /api/v1/work-orders

Request:
{
    "material_id": 100001,
    "routing_id": 200001,
    "planned_qty": 500,
    "plan_start_time": "2026-05-10T08:00:00",
    "plan_end_time": "2026-05-12T18:00:00",
    "priority": 50,
    "source_type": "ERP",
    "source_ref": "PO-2026-05001",
    "remark": "加急订单"
}

Response:
{
    "code": 0,
    "message": "success",
    "data": {
        "id": 300001,
        "order_no": "MO-20260508-0001",
        "status": "PENDING",
        "created_at": "2026-05-08T12:00:00"
    }
}
```

### 报工

```
POST /api/v1/work-reports

Request:
{
    "work_order_id": 300001,
    "step_id": 400001,
    "workstation_id": 500001,
    "operator_id": "USER001",
    "report_type": "COMPLETE",
    "good_qty": 480,
    "scrap_qty": 5,
    "rework_qty": 15,
    "duration_min": 120,
    "report_time": "2026-05-08T14:30:00",
    "remark": "正常完工"
}
```

---

## 10. AI 智能接口

### 10.1 质量预警

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /ai/quality/alerts | 获取活跃预警列表 |
| POST | /ai/quality/analyze | 执行质量分析（可选指定工单ID） |
| POST | /ai/quality/alerts/{id}/process | 处理预警（标记已处理） |
| GET | /ai/quality/history | 分页查询预警历史 |

#### 获取活跃预警

```
GET /api/v1/ai/quality/alerts

Response:
{
    "code": 0,
    "message": "success",
    "data": [
        {
            "id": 1,
            "rule_name": "产线连续不良率飙升",
            "title": "产线连续不良率飙升",
            "message": "产线(LineId=1)连续3个工单报废率超过5%",
            "level": "High",
            "related_entity_type": "ProductionLine",
            "related_entity_id": 1,
            "created_at": "2026-06-09T10:00:00Z",
            "is_processed": false
        }
    ]
}
```

### 10.2 智能排程

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /ai/scheduling/recommend/{workOrderId} | 获取排程推荐（Top 3） |

#### 获取排程推荐

```
GET /api/v1/ai/scheduling/recommend/300001

Response:
{
    "code": 0,
    "message": "success",
    "data": [
        {
            "line_id": 2,
            "line_name": "SMT-LINE-02",
            "confidence": 0.87,
            "suggested_start_time": "2026-06-10T08:00:00Z",
            "suggested_end_time": "2026-06-10T16:30:00Z",
            "reason": "产线负荷较低；历史完成匹配度高；交期紧急",
            "load_ratio": 0.45,
            "historical_match_score": 0.92
        }
    ]
}
```

> **评分算法**：产线负荷(40%) + 历史完成匹配(30%) + 交期紧急度(20%) + 工艺匹配度(10%)

### 10.3 设备预测

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /ai/equipment/health | 获取所有设备健康�� |
| GET | /ai/equipment/health/{equipmentId} | 获取单设备健康分析 |
| GET | /ai/equipment/high-risk | 获取高风险设备列表 |

#### 获取设备健康分析

```
GET /api/v1/ai/equipment/health/1

Response:
{
    "code": 0,
    "message": "success",
    "data": {
        "equipment_id": 1,
        "equipment_name": "贴片机A1",
        "health_score": 85,
        "risk_level": "良",
        "oee_7day_trend": -0.008,
        "oee_30day_trend": -0.003,
        "predicted_maintenance_date": "2026-06-20T00:00:00Z",
        "recommendation": "设备OEE呈下降趋势，建议关注运行状态并增加巡检频次"
    }
}
```

> **健康度评分规则**：
> - 90-100: 优（常规监控）
> - 70-89: 良（关注趋势）
> - 50-69: 预警（近期维护）
> - <50: 危险（立即检修）
