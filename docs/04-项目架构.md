# MES 系统架构设计

## 1. 整体架构（分层）

```
┌─────────────────────────────────────────────────────┐
│                    前端应用层                         │
│  Web (Vue/React)  │  PDA/移动端  │  大屏看板         │
└──────────────────────┬──────────────────────────────┘
                       │ HTTP/WebSocket
┌──────────────────────▼──────────────────────────────┐
│                    API 网关层                         │
│  认证鉴权  │  限流  │  路由转发  │  接口文档(Swagger) │
└──────────────────────┬──────────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────────┐
│                   业务服务层                          │
│  ┌─────────┐ ┌──────────┐ ┌─────────┐ ┌────────┐   │
│  │ 工单服务 │ │ 报工服务 │ │ 质检服务 │ │设备服务│   │
│  └─────────┘ └──────────┘ └─────────┘ └────────┘   │
│  ┌─────────┐ ┌──────────┐ ┌─────────┐ ┌────────┐   │
│  │基础数据  │ │ 追溯服务  │ │ 看板服务 │ │ 接口服务│   │
│  └─────────┘ └──────────┘ └─────────┘ └────────┘   │
└──────────────────────┬──────────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────────┐
│                   数据持久层                          │
│  关系数据库(PostgreSQL)  │  Redis缓存  │  文件存储   │
└──────────────────────┬──────────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────────┐
│                   数据采集层                          │
│  PLC采集  │  RFID扫码  │  IoT网关  │  手工录入       │
└─────────────────────────────────────────────────────┘
```

## 2. 技术架构推荐方案

### 方案A：.NET 10 + Vue 3（推荐）

| 层级 | 技术选型 |
|------|---------|
| 后端框架 | ASP.NET Core 10 |
| ORM | EF Core 10 |
| 认证 | JWT + Identity |
| 前端 | Vue 3 + Element Plus + TypeScript |
| 状态管理 | Pinia |
| 图表 | ECharts / AntV |
| 数据库 | PostgreSQL + SQL Sugar |
| 缓存 | Redis |
| 消息队列 | RabbitMQ |
| 实时通信 | SignalR（看板推送） |
| 文档 | Swagger / NSwag |

### 方案B：Java Spring Boot + React

| 层级 | 技术选型 |
|------|---------|
| 后端框架 | Spring Boot 3.x |
| ORM | MyBatis-Plus |
| 安全 | Spring Security + JWT |
| 前端 | React + Ant Design + TypeScript |
| 数据库 | MySQL 8 |
| 缓存 | Redis |
| 消息队列 | RocketMQ / RabbitMQ |

## 3. 核心设计模式

### 3.1 工单状态机

```
PENDING → RELEASED → SCHEDULED → IN_PROGRESS → COMPLETED → CLOSED
  ↓          ↓           ↓              ↓
CANCELLED  CANCELLED  CANCELLED      ON_HOLD → IN_PROGRESS
                                      ↓
                                   CANCELLED
```

状态转换校验：每种状态只能按预定义的方向流转，非法操作抛异常。

### 3.2 领域事件驱动

```
工单完工 → 触发 QualityCheckNeeded 事件 → 创建质检任务
工单完工 → 触发 MaterialConsume 事件    → 扣减物料库存
质检完成 → 触发 InventoryUpdate 事件    → 更新成品库存
报工完成 → 触发 ProductionOutput 事件   → 推送看板
```

## 4. 部署架构

```
                    ┌──────────┐
                    │  Nginx/LB │
                    └─���──┬─────┘
                  ┌──────┴──────┐
        ┌────────▼────┐  ┌────▼────────┐
        │ API Server 1│  │ API Server 2│
        └────────┬────┘  └────┬────────┘
                  └──────┬──────┘
                    ┌────▼────┐
                    │ PostgreSQL│ (主从)
                    └─────────┘
        ┌──────────┐  ┌──────┐  ┌──────────┐
        │  Redis   │  │RabbitMQ│  │  MinIO  │
        └──────────┘  └──────┘  └──────────┘
```

## 5. 扩展性设计

- **多租户**: 按工厂 tenant_id 隔离数据（推荐共享数据库 + 行级隔离）
- **插件化**: 业务逻辑预留扩展点（如报工前校验接口、工单审批链）
- **外部集成**: 通过消息队列解耦与 ERP/WMS/SCADA 的数据交换
